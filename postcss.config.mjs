// PostCSS config for Next.js (Tailwind v4)
// Vitest has its own postcss override in vitest.config.mjs, so this file
// only affects Next.js builds.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
